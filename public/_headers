# Cloudflare Workers static-assets header rules.
# Next.js build output under /_next/static/* has content-hashed filenames,
# so it is safe to cache immutably (a new build produces new filenames).
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
